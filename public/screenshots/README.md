# Deck Screenshots

This folder contains only presentation-ready images referenced by `src/deck/content.ts`.

Import a newly formatted screenshot with:

```powershell
npm run import:screenshot -- "C:\path\to\capture.png" "03-filter-response"
```

The image will be copied to `public/screenshots/deck/`. Keep raw and historical Discord images in `sources/discord-convo/images/`.
